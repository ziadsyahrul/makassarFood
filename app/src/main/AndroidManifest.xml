<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ziadsyahrul.makassarfood">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_aplikasi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity" />
        <activity android:name=".RecycleActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".RekomendasiActivity" />
        <activity android:name=".RecycleRekomendasiActivity" />
        <activity android:name=".KritikSaranActivity" />
        <activity android:name=".DetailResepActivity" />
    </application>

</manifest>